export const physicsConfig = Object.freeze({
    default: 'arcade',
    arcade: {
        gravity: {
            x: 0,
            y: 0
        },
        debug: true,
    }
});

export const PLAYER = Object.freeze({
    SPEED: 300,
});
